INSTALLED_APPS = ['nsapp', 'checkout.apps.CheckoutConfig', 'weird.WeirdConfig']

TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [],
        'APP_DIRS': True,
    }
]
